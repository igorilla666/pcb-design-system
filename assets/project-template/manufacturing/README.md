# Manufacturing

Create one immutable directory per released hardware revision and assembly
variant. Keep Gerbers, drill files, BOM, CPL, manual assembly list, validation
report, and checksums together.

Never mix files from different variants even when filenames are identical.
