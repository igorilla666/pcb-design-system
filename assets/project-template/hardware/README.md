# Hardware

Keep each hardware revision or production variant in an independent directory.
Do not overwrite a frozen baseline.

Suggested layout:

`baseline/`
`revisions/rev-a/`
`variants/jlcpcb-standard/`
`variants/jlcpcb-economic/`
